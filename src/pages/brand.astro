---
/**
 * Brand — One System. The site's living brand reference, rebuilt from the
 * authoritative One-System-Brand-Guidelines (Drive) rather than the early
 * mockup. Rules are transcribed from that source of truth; tokens (color,
 * spacing, radius, motion) are read LIVE from src/styles/tokens.css at build
 * time and rendered with var(), so the page can't drift from the real system.
 *
 * Source of truth: One-System-Brand-Guidelines v1.0. Voice rules come from
 * System Master (the brand doc defers voice to it; summarized per CLAUDE.md).
 * The Formats section (social / reel / lower-third) is ported as live mockups
 * rendered from tokens at true export dimensions. Chrome from Base.astro /
 * global.css.
 */
import Base from '../layouts/Base.astro';
import Brandmark from '../components/Brandmark.astro';
// Token file read as a raw string at build time (Vite ?raw) — no node:fs,
// so the page bundles cleanly for the Cloudflare Worker runtime.
import tokensCss from '../styles/tokens.css?raw';
const tokenValue = (name: string) => {
  const m = tokensCss.match(new RegExp(`${name}\\s*:\\s*([^;]+);`));
  return m ? m[1].trim() : '';
};
const hex = (name: string) => tokenValue(name).replace('#', '').toUpperCase();
const px = (name: string) => tokenValue(name).replace('px', '');

// Live ramps + scales — every value is read from the token file.
const neutral = ['--n-0', '--n-50', '--n-100', '--n-200', '--n-300', '--n-400', '--n-500', '--n-550', '--n-600', '--n-700', '--n-800', '--n-900', '--n-950'];
const accent = ['--accent-50', '--accent-100', '--accent-200', '--accent-300', '--accent-400', '--accent-500', '--accent-600', '--accent-700', '--accent-800', '--accent-900'];
const spacing = [
  { token: '--space-xs', name: 'xs' }, { token: '--space-sm', name: 'sm' },
  { token: '--space-md', name: 'md' }, { token: '--space-lg', name: 'lg' },
  { token: '--space-xl', name: 'xl' }, { token: '--space-2xl', name: '2xl' },
  { token: '--space-3xl', name: '3xl' }, { token: '--space-4xl', name: '4xl' },
];
const radii = [
  { token: '--radius-none', name: 'none' }, { token: '--radius-sm', name: 'sm' },
  { token: '--radius-md', name: 'md', star: true }, { token: '--radius-lg', name: 'lg' },
  { token: '--radius-xl', name: 'xl' }, { token: '--radius-full', name: 'full' },
];
const durations = [
  { token: '--dur-fast', name: 'fast', w: 48 }, { token: '--dur-base', name: 'base', w: 80, star: true },
  { token: '--dur-slow', name: 'slow', w: 128 }, { token: '--dur-deliberate', name: 'deliberate', w: 200 },
];

const stepOf = (t: string) => t.replace(/--(n|accent)-/, '');
---

<Base
  image="/og/brand.png"
  title="Brand · One System · Pratik Mehta"
  description="One System is the design system this site runs on: foundation, logo, type, color, layout, motion, and access. Rendered from the live tokens, so it can't drift."
  activeNav="brand"
>
  <header class="bhero">
    <div class="page-kicker rev">[ Brand ] · One System · v1.0</div>
    <h1 class="rev page-h1">The brand,<br />built like a <span class="signal">product.</span></h1>
    <p class="lead rev">One System is the identity this site runs on: foundation, logo, type, color, layout, motion, and access, in one place. It is a working reference and a case study of the thinking behind it. The page renders from the live tokens, so it can't drift from the real site.</p>
  </header>

  {/* 01 FOUNDATION */}
  <section class="bsec">
    <div class="blabel"><span class="c">[ 01 ]</span><h2 class="t">Foundation · the spine</h2><span class="rule"></span></div>
    <p class="intro">Creative built like a system and accountable to what it does after it ships. The breadth is the proof of the idea, not a separate selling point.</p>
    <div class="frow" style="border-top:none">
      <div class="fspec">tagline</div>
      <div class="fsamp tagline">One system, end to end<span class="signal">.</span></div>
    </div>
    <div class="frow"><div class="fspec">reserve line</div><div class="fsamp">Built, not briefed.</div></div>
    <div class="frow"><div class="fspec">accent signal</div><div class="fsamp mono">Signal orange · #{hex('--accent-500')} · used once, to do one job</div></div>
  </section>

  {/* 02 LOGO */}
  <section class="bsec">
    <div class="blabel"><span class="c">[ 02 ]</span><h2 class="t">Logo · the loop</h2><span class="rule"></span></div>
    <p class="intro">One continuous stroke that closes on the orange node: the system, end to end, as a mark. Default to the primary lockup. The wordmark carries the circular period; the lockups don't.</p>
    <div class="logoset rev">
      <div class="tile"><div class="lockrow"><Brandmark size={52} title={undefined} /><span class="wm">Pratik Mehta</span></div></div>
      <div class="tile dark"><div class="lockrow"><Brandmark size={52} title={undefined} /><span class="wm">Pratik Mehta</span></div></div>
      <div class="tile"><div class="lockcol"><Brandmark size={44} title={undefined} /><span class="wm" style="font-size:24px">Pratik Mehta</span></div></div>
      <div class="tile"><span class="wm" style="font-size:31px">Pratik Mehta<span class="period"></span></span></div>
    </div>
    <div class="cap">primary (ink / paper) · stacked · wordmark with period. Use the mono mark where the accent can't appear.</div>

    <div class="subhead">Clear space &amp; minimum size</div>
    <div class="clearrow">
      <div><div class="csbox"><Brandmark size={72} title={undefined} /></div><div class="cap">keep-out = 0.25 × mark height</div></div>
      <div class="mincol"><Brandmark size={24} title={undefined} /><div class="cap">mark · 24px floor</div></div>
      <div class="mincol"><Brandmark size={16} title={undefined} /><div class="cap">favicon cut · 16px</div></div>
    </div>

    <div class="subhead">Never do this</div>
    <div class="dont rev">
      <div class="card dcard"><span class="x">✕</span><div class="dwrap"><div class="d-stretch"><Brandmark size={52} title={undefined} /></div><span class="dlab">stretch or distort</span></div></div>
      <div class="card dcard"><span class="x">✕</span><div class="dwrap"><div class="d-recolor"><Brandmark size={52} title={undefined} /></div><span class="dlab">recolor the node</span></div></div>
      <div class="card dcard"><span class="x">✕</span><div class="dwrap"><span class="wm" style="font-size:24px">Pratik Mehta<span class="period sq"></span></span><span class="dlab">square the period</span></div></div>
      <div class="card dcard"><span class="x">✕</span><div class="dwrap"><div class="d-shadow"><Brandmark size={52} title={undefined} /></div><span class="dlab">add effects</span></div></div>
      <div class="card dcard"><span class="x">✕</span><div class="dwrap"><div class="d-rotate"><Brandmark size={52} title={undefined} /></div><span class="dlab">rotate or tilt</span></div></div>
      <div class="card dcard"><span class="x">✕</span><div class="dwrap"><div class="d-onaccent"><Brandmark size={40} title={undefined} /></div><span class="dlab">accent node on orange</span></div></div>
    </div>
  </section>

  {/* 03 TYPE */}
  <section class="bsec">
    <div class="blabel"><span class="c">[ 03 ]</span><h2 class="t">Type · three faces, one system</h2><span class="rule"></span></div>
    <p class="intro">Each face has one job. Clash Display is the voice: display, h1, h2. Clash Grotesk is reading type: sub-headings and body. JetBrains Mono is the system layer: labels, data, and code. All three ship as one variable file per family, so every weight below is real rather than the nearest available one.</p>
    <p class="intro">Tracking is optical, not decorative. It tightens as type gets bigger, sits at zero for reading, and opens up for uppercase mono. Mono labels and emphasis are always uppercase, because heavy tracking is an uppercase device and breaks apart on lowercase; sentence-case mono is the data tier at +5% instead.</p>
    {/* A spec is tabular data, so it renders as a table: tier / face / weight /
        tracking each get a column and nothing can wrap. It was one 190px column
        holding all four as a single string, which broke "Clash Display" across
        lines and split "JetBrains Mono" in half.
        Every sample is a REAL use of that tier on this site — the samples are the
        proof, so an invented one would make this page lie about its own system.
        The `where` column names the consumer; keep it honest if a tier moves. */}
    <table class="tyt">
      <thead>
        <tr><th>tier</th><th>face</th><th>wt</th><th>track</th><th>where</th><th class="s">sample</th></tr>
      </thead>
      <tbody>
        <tr>
          <td class="tier">display</td><td class="meta">Clash Display</td><td class="meta">700</td><td class="meta">-2%</td>
          <td class="meta">every page head, closing CTA, stat figures</td>
          <td class="s"><span class="v disp" style="font-size:clamp(28px,3.4vw,42px)">One system<span class="signal">.</span></span></td>
        </tr>
        <tr>
          <td class="tier">h1</td><td class="meta">Clash Display</td><td class="meta">600</td><td class="meta">-1%</td>
          <td class="meta">the wordmark lockup</td>
          <td class="s"><span class="v t-h1" style="font-size:22px">Pratik Mehta</span></td>
        </tr>
        <tr>
          <td class="tier">h2</td><td class="meta">Clash Display</td><td class="meta">500</td><td class="meta">-1%</td>
          <td class="meta">section heads, journal lead post, mobile nav</td>
          <td class="s"><span class="v t-h2" style="font-size:24px">Engineer turned creative.</span></td>
        </tr>
        <tr>
          <td class="tier">h3-h6</td><td class="meta">Clash Grotesk</td><td class="meta">500</td><td class="meta">0%</td>
          <td class="meta">card titles, index rows, sub-heads</td>
          <td class="s"><span class="v t-h3" style="font-size:19px">SPORTIME Clubs</span></td>
        </tr>
        <tr>
          <td class="tier">body</td><td class="meta">Clash Grotesk</td><td class="meta">400</td><td class="meta">0%</td>
          <td class="meta">copy · lh 1.65 · <strong style="font-weight:600;color:var(--text-secondary)">strong</strong> = 600</td>
          <td class="s"><span class="v t-body" style="font-size:15px;line-height:1.6;color:var(--text-secondary)">The work stays coherent and the loop closes.</span></td>
        </tr>
        <tr>
          <td class="tier">label</td><td class="meta">JetBrains Mono</td><td class="meta">400</td><td class="meta">+10%</td>
          <td class="meta">kickers, badges, section labels · always uppercase</td>
          <td class="s"><span class="v t-label" style="font-size:11px;color:var(--text-secondary)">Brand · Social · Performance</span></td>
        </tr>
        <tr>
          <td class="tier">data / nav</td><td class="meta">JetBrains Mono</td><td class="meta">400</td><td class="meta">+5%</td>
          <td class="meta">rail nav, scoreboard values, the clock · sentence case</td>
          <td class="s"><span class="v t-data" style="font-size:13px;color:var(--text-secondary)">Home &nbsp;·&nbsp; 2023-2024 &nbsp;·&nbsp; 12:53 PM</span></td>
        </tr>
        <tr>
          <td class="tier">emphasis</td><td class="meta">JetBrains Mono</td><td class="meta">600</td><td class="meta">+10%</td>
          <td class="meta">buttons · always uppercase</td>
          <td class="s"><span class="v t-emphasis" style="font-size:11px">View work</span></td>
        </tr>
      </tbody>
    </table>
  </section>

  {/* 04 COLOR */}
  <section class="bsec">
    <div class="blabel"><span class="c">[ 04 ]</span><h2 class="t">Color · warm monochrome, one accent</h2><span class="rule"></span></div>
    <p class="intro">A warm neutral ramp from paper to ink, a single signal-orange accent, and a restrained state set. Both light and dark are first-class.</p>
    <div class="subhead">Neutral · warm ramp</div>
    <div class="ramp">
      {neutral.map((t) => (
        <div class="chip"><div class="sw" style={`background: var(${t})`}></div><div class="hx">{stepOf(t)}<br />{hex(t)}</div></div>
      ))}
    </div>
    <div class="subhead">Accent · signal orange <span class="signal">★ 500</span></div>
    <div class="ramp">
      {accent.map((t) => (
        <div class="chip"><div class="sw" style={`background: var(${t});${t === '--accent-500' ? ' outline:2px solid var(--text); outline-offset:1px;' : ''}`}></div><div class="hx">{stepOf(t)}{t === '--accent-500' ? '★' : ''}<br />{hex(t)}</div></div>
      ))}
    </div>
    <div class="subhead">Surfaces · light &amp; dark</div>
    <div class="panels">
      <div class="panel" style="background:var(--n-50)"><div class="p-lab" style="color:var(--n-550)">light · paper</div><div class="p-h" style="color:var(--n-950)">One system<span style="color:var(--accent-500)">.</span></div><div class="p-b" style="color:var(--n-600)">Body in graphite on paper. The accent does one job.</div><span class="p-pill" style="background:var(--accent-50);color:var(--accent-800)">brand · social · perf</span></div>
      <div class="panel" style="background:var(--n-950);border-color:var(--n-800)"><div class="p-lab" style="color:var(--n-400)">dark · ink</div><div class="p-h" style="color:var(--n-50)">One system<span style="color:var(--accent-500)">.</span></div><div class="p-b" style="color:var(--n-400)">Same system inverted. Orange stays the single accent.</div><span class="p-pill" style="background:var(--n-800);color:var(--accent-400)">brand · social · perf</span></div>
    </div>
  </section>

  {/* 05 LAYOUT */}
  <section class="bsec">
    <div class="blabel"><span class="c">[ 05 ]</span><h2 class="t">Layout · a 4px base</h2><span class="rule"></span></div>
    <p class="intro">Everything measures from a 4px unit. Layout is a {px('--grid-cols')}-column grid, max-width {px('--grid-max-width')}, {px('--grid-gutter')} gutters, {px('--grid-margin')} margins. Radius base is {px('--radius-md')}px; the mono system layer uses sharp 0 corners.</p>
    <div class="scale">
      {spacing.map((s) => (
        <div class="s-row"><div class="s-lab">{s.name} · {tokenValue(s.token)}</div><div class="s-bar" style={`width: var(${s.token})`}></div></div>
      ))}
    </div>
    <div class="card grid-demo" aria-hidden="true">{Array.from({ length: 12 }).map(() => (<div class="gcol"></div>))}</div>
    <div class="mono muted gridnote">{px('--grid-cols')} columns · max {px('--grid-max-width')} · gutter {px('--grid-gutter')} · margin {px('--grid-margin')}</div>
    <div class="subhead" style="margin-top:var(--space-xl)">Radius</div>
    <div class="r-row">
      {radii.map((r) => (
        <div><div class="r-box" style={`border-radius: var(${r.token})`}></div><div class="hx">{r.name}{r.star ? ' ★' : ''} · {r.name === 'full' ? 'pill' : px(r.token)}</div></div>
      ))}
    </div>
  </section>

  {/* 06 MOTION */}
  <section class="bsec">
    <div class="blabel"><span class="c">[ 06 ]</span><h2 class="t">Motion · functional, not decorative</h2><span class="rule"></span></div>
    <p class="intro">Quick and confident. One ease-out curve, four durations, short travel, no bounce. Reels inherit this: cuts over fades, snap transitions.</p>
    <div class="motion">
      <div class="durs">
        {durations.map((d) => (
          <div class="d-row"><div class="d-lab">{d.name} · {tokenValue(d.token)}{d.star ? ' ★' : ''}</div><div class="d-bar" style={`width:${d.w}px`}></div></div>
        ))}
        <div class="mono muted" style="font-size:10.5px;margin-top:var(--space-md)">ease · {tokenValue('--ease-standard')}</div>
      </div>
      <div class="hoverdemo"><a class="btn btn-p" href="/work">View work</a><div class="mono muted" style="font-size:10.5px">Hover</div></div>
    </div>
  </section>

  {/* 07 ACCESSIBILITY */}
  <section class="bsec">
    <div class="blabel"><span class="c">[ 07 ]</span><h2 class="t">Accessibility · tuned to WCAG AA</h2><span class="rule"></span></div>
    <p class="intro">The single orange signal can't sit on top of itself. Three rules carry most of it.</p>
    <div class="rules rev">
      <div class="rule-card"><div class="rule-k">Orange text</div><div class="rule-b">On light, orange text is accent-700 (#{hex('--accent-700')}), never 500. On dark, accent-400.</div></div>
      <div class="rule-card"><div class="rule-k">On orange fills</div><div class="rule-b">Text and the logo on an orange surface are ink, never white. The mark goes monochrome so the node doesn't blend.</div></div>
      <div class="rule-card"><div class="rule-k">State colors</div><div class="rule-b">State fills for indicators; the darker -text variants when used as text.</div></div>
    </div>
    <table>
      <thead><tr><th>Pair</th><th>Ratio</th><th>Verdict</th></tr></thead>
      <tbody>
        <tr><td>Ink on paper</td><td class="r">17.6:1</td><td class="aa">AAA</td></tr>
        <tr><td>Secondary #615D52 on paper</td><td class="r">5.9:1</td><td class="aa">AA</td></tr>
        <tr><td>Accent-700 text on paper</td><td class="r">4.8:1</td><td class="aa">AA</td></tr>
        <tr><td>Ink on orange fill</td><td class="r">6.3:1</td><td class="aa">AA</td></tr>
        <tr><td>Paper on ink (dark)</td><td class="r">17.6:1</td><td class="aa">AAA</td></tr>
      </tbody>
    </table>
  </section>

  {/* 08 FORMATS */}
  <section class="bsec">
    <div class="blabel"><span class="c">[ 08 ]</span><h2 class="t">Formats · the brand, in format</h2><span class="rule"></span></div>
    <p class="intro">How the system shows up as social posts, reel frames, and a lower-third. Placeholder content at true export dimensions, rendered live so the mockups re-theme with the page.</p>

    <div class="subhead">Social · 1080 × 1080</div>
    <div class="fmt-stage">
      <div class="unit">
        <div class="fr w360"><div class="b1x1 bg-paper"><div class="fmt-pad">
          <div class="kick k-acc">// placeholder label</div>
          <div class="fmt-disp t-ink" style="font-size:128px;line-height:0.98">One system,<br />end to end<span class="t-acc">.</span></div>
          <div class="ffoot"><div class="foot2 k-mut">mehtapratik.com</div><Brandmark size={104} title={undefined} class="fmt-mk-ink" /></div>
        </div></div></div>
        <div class="spec"><b>feed</b> · statement</div>
      </div>
      <div class="unit">
        <div class="fr w360"><div class="b1x1 bg-ink"><div class="fmt-pad">
          <div class="kick k-acc">// placeholder label</div>
          <div class="fmt-disp t-paper" style="font-size:104px;line-height:1.0">Placeholder<br />headline goes<br />right here<span class="t-acc">.</span></div>
          <div class="ffoot"><div class="foot2 k-paper">@pratikm96</div><Brandmark size={104} title={undefined} class="fmt-mk-paper" /></div>
        </div></div></div>
        <div class="spec"><b>feed</b> · inverted</div>
      </div>
      <div class="unit">
        <div class="fr w360"><div class="b1x1 bg-paper"><div class="fmt-pad">
          <div class="kick k-mut">metric · placeholder</div>
          <div><div class="fmt-disp t-acc" style="font-size:240px;line-height:0.9">00.0</div><div class="kick k-ink" style="font-size:30px;margin-top:var(--space-md)">metric label goes here</div></div>
          <div class="ffoot"><div class="foot2 k-mut">mehtapratik.com</div><Brandmark size={104} title={undefined} class="fmt-mk-ink" /></div>
        </div></div></div>
        <div class="spec"><b>feed</b> · data</div>
      </div>
    </div>

    <div class="subhead">Reel / story · 1080 × 1920</div>
    <div class="fmt-stage">
      <div class="unit">
        <div class="fr w270"><div class="b9x16 bg-ink"><div class="fmt-pad fmt-pad-v" style="justify-content:space-between">
          <div class="kick k-acc">reel · intro frame</div>
          <div><div class="fmt-disp t-paper" style="font-size:150px;line-height:0.96">Built,<br />not<br />briefed<span class="t-acc">.</span></div><div class="rule8" style="margin-top:var(--space-2xl)"></div></div>
          <div class="ffoot"><div class="foot2 k-paper" style="line-height:1.5">placeholder<br />caption line</div><Brandmark size={104} title={undefined} class="fmt-mk-paper" /></div>
        </div></div></div>
        <div class="spec"><b>reel</b> · intro</div>
      </div>
      <div class="unit">
        <div class="fr w270"><div class="b9x16 bg-accent"><div class="fmt-pad fmt-pad-v" style="justify-content:space-between">
          <div class="kick k-ink">end card</div>
          <div class="fmt-disp t-ink" style="font-size:140px;line-height:0.96">Let's<br />build<span style="opacity:0.55">.</span></div>
          <div class="ffoot"><div class="foot2 t-ink" style="line-height:1.5">mehtapratik.com<br />@pratikm96</div><Brandmark size={104} title={undefined} class="fmt-mk-mono" /></div>
        </div></div></div>
        <div class="spec"><b>reel</b> · end card · accent surface → mono mark</div>
      </div>
    </div>

    <div class="subhead">Lower-third · 1920 × 1080</div>
    <div class="fmt-stage">
      <div class="unit">
        <div class="fr w480"><div class="b16x9 lt-bg">
          <div class="lt-ph">▢ video placeholder</div>
          <div class="lt-bar"><div class="lt-edge"></div><div class="lt-box"><div class="lt-name">Pratik Mehta</div><div class="lt-title">Creative Marketing Lead</div></div></div>
        </div></div>
        <div class="spec"><b>caption</b> · over video</div>
      </div>
    </div>
  </section>

  {/* 09 VOICE */}
  <section class="bsec">
    <div class="blabel"><span class="c">[ 09 ]</span><h2 class="t">Voice · source: System Master</h2><span class="rule"></span></div>
    <p class="intro">Direct, specific, natural, not over-polished, not obviously AI-written. Lead with the answer. No inflated claims, no buzzwords, no unsupported proof. No em or en dashes.</p>
    <div class="voice rev">
      <div class="card vcol do"><div class="h">Do</div><ul><li>Lead with the answer</li><li>Name real numbers, verified</li><li>Plain, confident sentences</li><li>Label concept work clearly</li></ul></div>
      <div class="card vcol dont-col"><div class="h">Don't</div><ul><li>Inflate or overclaim</li><li>Reach for buzzwords</li><li>Over-polish into AI-speak</li><li>Imply concepts are client work</li></ul></div>
    </div>
  </section>
</Base>

<style>
  /* type tiers — family/weight/tracking only; rules below set size + colour */
  .fsamp.tagline,
  .wm,
  .tyt .v.disp,
  .fmt-disp {
    font-family: var(--t-display-font); font-weight: var(--t-display-weight); letter-spacing: var(--t-display-track);
  }
  .p-h,
  .lt-name {
    font-family: var(--t-h3-font); font-weight: var(--t-h3-weight); letter-spacing: var(--t-h3-track);
  }
  .subhead,
  .tyt th,
  .tyt .tier,
  .p-lab,
  th,
  .rule-k,
  .vcol .h,
  .kick {
    font-family: var(--t-label-font); font-weight: var(--t-label-weight); letter-spacing: var(--t-label-track);
  }
  .cap,
  .mono,
  .fspec,
  .fsamp.mono,
  .x,
  .dlab,
  .tyt .meta,
  .hx,
  .p-pill,
  .s-lab,
  .d-lab,
  td.r,
  .aa,
  .spec,
  .foot2,
  .lt-ph,
  .lt-title {
    font-family: var(--t-data-font); font-weight: var(--t-data-weight); letter-spacing: var(--t-data-track);
  }

  .bhero { padding: var(--space-4xl) 0 var(--space-3xl); }
  /* type + measure come from the shared .lead primitive in global.css */
  .bhero p { margin-top: var(--space-lg); }
  .bsec { padding: var(--space-4xl) 0; }
  .bsec .intro { font-size: 16px; line-height: 1.6; color: var(--text-secondary); max-width: 66ch; margin-bottom: var(--space-xl); }
  .subhead { font-size: 11px; text-transform: uppercase; color: var(--text-muted); margin: var(--space-2xl) 0 var(--space-md); }
  .cap { font-size: 11px; color: var(--text-muted); margin-top: var(--space-sm); line-height: 1.5; }
  .muted { color: var(--text-muted); }

  /* foundation */
  .frow { display: flex; align-items: baseline; gap: var(--space-lg); padding: var(--space-md) 0; border-top: 1px solid var(--border); flex-wrap: wrap; }
  .fspec { font-size: 11px; color: var(--text-muted); width: 200px; flex-shrink: 0; }
  .fsamp { flex: 1; min-width: 240px; color: var(--text); font-size: 16px; }
  .fsamp.tagline { font-size: 24px; }
  .fsamp.mono { font-size: 14px; }

  /* logo */
  .logoset { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-md); margin-bottom: var(--space-md); }
  .tile { background: var(--n-0); border: 1px solid var(--border); border-radius: var(--radius-lg); padding: var(--space-xl); display: flex; align-items: center; justify-content: center; min-height: 140px; }
  .tile.dark { background: var(--n-950); border-color: var(--n-800); }
  .tile :global(.brandmark) { color: var(--n-950); }
  .tile.dark :global(.brandmark) { color: var(--n-50); }
  .lockrow { display: flex; align-items: center; gap: var(--space-md); }
  .lockcol { display: flex; flex-direction: column; align-items: center; gap: var(--space-sm); }
  .wm { font-size: 30px; color: var(--n-950); }
  .tile.dark .wm { color: var(--n-50); }
  .period { display: inline-block; width: .16em; height: .16em; border-radius: 50%; background: var(--accent-fill); vertical-align: baseline; margin-left: .04em; }
  .period.sq { border-radius: 0; }
  .clearrow { display: flex; align-items: flex-end; gap: var(--space-2xl); flex-wrap: wrap; }
  .clearrow :global(.brandmark) { color: var(--text); }
  .csbox { display: inline-block; border: 1px dashed var(--accent-fill); padding: var(--space-lg); border-radius: var(--radius-sm); }
  .mincol { display: flex; flex-direction: column; align-items: center; gap: var(--space-sm); }

  /* don'ts */
  .dont { display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--space-md); margin-top: var(--space-xs); }
  /* surface comes from global .card */
  .dcard { padding: var(--space-lg); position: relative; min-height: 124px; display: flex; align-items: center; justify-content: center; }
  .dcard :global(.brandmark) { color: var(--text); }
  .x { position: absolute; top: 12px; left: 12px; width: 20px; height: 20px; border-radius: 50%; background: var(--danger); color: #fff; font-size: 12px; line-height: 20px; text-align: center; }
  .dwrap { display: flex; flex-direction: column; align-items: center; gap: var(--space-sm); }
  .dlab { font-size: 10.5px; color: var(--text-secondary); text-align: center; line-height: 1.5; }
  .d-stretch :global(.brandmark) { transform: scaleX(1.7); }
  .d-recolor :global(.node) { fill: #2A4DFF; }
  .d-shadow :global(.brandmark) { filter: drop-shadow(3px 4px 3px rgba(0, 0, 0, .4)); }
  .d-rotate :global(.brandmark) { transform: rotate(15deg); }
  .d-onaccent { background: var(--accent-fill); padding: var(--space-md); border-radius: var(--radius-md); display: flex; }

  /* type — a spec table: one column per datum, so nothing wraps and the weight
     ladder (700 / 600 / 500 / 400) is scannable straight down a column. Replaced a
     190px label column holding tier+face+weight+tracking as one string, which
     wrapped mid-font-name. */
  .tyt { width: 100%; border-collapse: collapse; table-layout: auto; }
  .tyt th {
    text-transform: uppercase; font-size: 9.5px; color: var(--text-muted);
    text-align: left; padding: 0 var(--space-md) var(--space-sm) 0; border-bottom: 1px solid var(--border); white-space: nowrap;
  }
  .tyt td { padding: var(--space-md) var(--space-md) var(--space-md) 0; border-bottom: 1px solid var(--border); vertical-align: middle; }
  .tyt .tier {
    text-transform: uppercase; font-size: 10.5px; color: var(--accent-text); white-space: nowrap;
  }
  /* face / wt / track never wrap — that was the whole bug */
  .tyt .meta {
    font-size: 10.5px; color: var(--text-muted); line-height: 1.5;
  }
  .tyt th:nth-child(2), .tyt td:nth-child(2),
  .tyt th:nth-child(3), .tyt td:nth-child(3),
  .tyt th:nth-child(4), .tyt td:nth-child(4) { white-space: nowrap; }
  .tyt .s { width: 42%; }
  .tyt .v { min-width: 0; color: var(--text); display: inline-block; }
  .tyt .v.disp { line-height: 1; }

  /* below the grid the `where` column is the first thing to go, then the spec
     columns collapse into the tier cell rather than squeezing to nothing */
  @media (max-width: 1100px) { .tyt th:nth-child(5), .tyt td:nth-child(5) { display: none; } }

  /* color */
  .ramp { display: flex; gap: var(--space-xs); flex-wrap: wrap; margin-bottom: var(--space-xs); }
  .chip { width: 62px; }
  .sw { height: 46px; border-radius: var(--radius-sm); border: 1px solid rgba(128, 128, 128, .18); }
  .hx { font-size: 9.5px; color: var(--text-muted); margin-top: var(--space-2xs); line-height: 1.4; }
  .panels { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-md); margin-top: var(--space-xs); }
  .panel { border-radius: var(--radius-lg); padding: var(--space-lg); border: 1px solid rgba(128, 128, 128, .18); }
  .p-lab { font-size: 10px; text-transform: uppercase; margin-bottom: var(--space-sm); }
  .p-h { font-size: 22px; margin-bottom: var(--space-xs); }
  .p-b { font-size: 13px; line-height: 1.55; margin-bottom: var(--space-md); }
  .p-pill { display: inline-block; font-size: 10px; padding: var(--space-xs) var(--space-sm); border-radius: var(--radius-sm); }

  /* layout */
  .s-row { display: flex; align-items: center; gap: var(--space-md); margin-bottom: var(--space-xs); }
  .s-lab { font-size: 11px; color: var(--text-secondary); width: 128px; flex-shrink: 0; }
  .s-bar { height: 14px; background: var(--accent-fill); border-radius: 2px; }
  /* 12-column grid demo (matches the brand guidelines Layout section) */
  /* surface comes from global .card */
  .grid-demo { display: flex; gap: var(--space-xs); padding: 0 var(--space-lg); height: 96px; margin-top: var(--space-xl); }
  .gcol { flex: 1; background: rgba(255, 90, 31, .10); border-left: 1px solid var(--accent-fill); border-right: 1px solid var(--accent-fill); }
  .gridnote { font-size: 10.5px; margin-top: var(--space-sm); }
  .r-row { display: flex; gap: var(--space-md); flex-wrap: wrap; }
  .r-box { width: 60px; height: 60px; background: var(--surface); border: 1.5px solid var(--text); }

  /* motion */
  .motion { display: flex; gap: var(--space-xl); flex-wrap: wrap; align-items: flex-start; }
  .durs { flex: 1; min-width: 240px; }
  .d-row { display: flex; align-items: center; gap: var(--space-md); margin-bottom: var(--space-xs); }
  .d-lab { font-size: 11px; color: var(--text-secondary); width: 160px; flex-shrink: 0; }
  .d-bar { height: 8px; background: var(--accent-fill); border-radius: 2px; }
  /* hover demo: label sits below and centered on the button (it lifts upward on hover) */
  .hoverdemo { display: flex; flex-direction: column; align-items: center; gap: var(--space-sm); }

  /* a11y */
  table { width: 100%; border-collapse: collapse; font-size: 13.5px; margin-top: var(--space-xs); }
  th, td { text-align: left; padding: var(--space-sm) var(--space-sm); border-bottom: 1px solid var(--border); }
  th { font-size: 11px; text-transform: uppercase; color: var(--text-muted); }
  td.r { color: var(--text); }
  .aa { color: var(--success-text); font-size: 12px; }
  .rules { display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--space-md); margin: var(--space-xs) 0 var(--space-xl); }
  .rule-card { border: 1px solid var(--border); border-left: 2px solid var(--accent-fill); border-radius: var(--radius-md); padding: var(--space-md); }
  .rule-k { font-size: 10px; text-transform: uppercase; color: var(--accent-text); margin-bottom: var(--space-xs); }
  .rule-b { font-size: 13.5px; color: var(--text-secondary); line-height: 1.55; }

  /* voice */
  .voice { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-md); }
  /* surface comes from global .card */
  .vcol { padding: var(--space-lg); }
  .vcol .h { font-size: 11px; text-transform: uppercase; margin-bottom: var(--space-md); }
  .vcol.do .h { color: var(--accent-text); }
  .vcol.dont-col .h { color: var(--text-muted); }
  .vcol ul { list-style: none; display: flex; flex-direction: column; gap: var(--space-sm); }
  .vcol li { font-size: 14px; color: var(--text-secondary); padding-left: var(--space-md); position: relative; }
  .vcol li::before { content: "·"; position: absolute; left: 2px; color: var(--accent-text); }

  /* formats — live mockups at true export dimensions (scaled previews) */
  .fmt-stage { display: flex; gap: var(--space-xl); flex-wrap: wrap; align-items: flex-start; }
  .unit { display: flex; flex-direction: column; gap: var(--space-sm); }
  .spec { font-size: 11px; color: var(--text-muted); }
  .spec b { color: var(--text); font-weight: 400; }
  .fr { overflow: hidden; border-radius: var(--radius-sm); border: 1px solid var(--border); }
  .w360 { width: 340px; height: 340px; }
  .w270 { width: 255px; height: 453px; }
  .w480 { width: 440px; height: 247px; }
  .b1x1 { width: 1080px; height: 1080px; transform-origin: top left; transform: scale(.31481); }
  .b9x16 { width: 1080px; height: 1920px; transform-origin: top left; transform: scale(.236); }
  .b16x9 { width: 1920px; height: 1080px; transform-origin: top left; transform: scale(.22917); }
  .bg-paper { background: var(--n-50); }
  .bg-ink { background: var(--n-950); }
  .bg-accent { background: var(--accent-fill); }
  .fmt-pad { padding: var(--space-4xl); height: 100%; display: flex; flex-direction: column; justify-content: space-between; }
  .fmt-pad-v { padding: var(--space-4xl) var(--space-4xl); }
  .ffoot { display: flex; justify-content: space-between; align-items: flex-end; }
  .kick { font-size: 26px; text-transform: uppercase; }
  .k-acc, .t-acc { color: var(--accent-700); }
  .bg-ink .k-acc, .bg-ink .t-acc { color: var(--accent-400); }
  .k-ink { color: var(--n-950); }
  .k-mut { color: var(--n-500); }
  .k-paper { color: var(--n-400); }
  .t-ink { color: var(--n-950); }
  .t-paper { color: var(--n-50); }

  .foot2 { font-size: 24px; }
  .rule8 { height: 8px; width: 200px; background: var(--accent-fill); }
  /* lower-third caption mockup */
  .lt-bg { background: repeating-linear-gradient(135deg, #C6C2B5 0 40px, #BDB9AC 40px 80px); position: relative; }
  .lt-ph { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); font-size: 32px; color: #7A7567; }
  .lt-bar { position: absolute; left: 90px; bottom: 120px; display: flex; align-items: stretch; }
  .lt-edge { width: 10px; background: var(--accent-fill); }
  .lt-box { background: rgba(11, 11, 10, .82); padding: var(--space-xl) var(--space-2xl) var(--space-xl) var(--space-2xl); }
  .lt-name { font-size: 62px; color: var(--n-50); line-height: 1; }
  .lt-title { font-size: 28px; color: #C6C2B5; margin-top: var(--space-md); }
  /* mockup mark color variants (override Brandmark's default ink/accent) */
  :global(.fmt-mk-ink) { color: var(--n-950) !important; }
  :global(.fmt-mk-paper) { color: var(--n-50) !important; }
  :global(.fmt-mk-mono) { color: var(--n-950) !important; }
  :global(.fmt-mk-mono .node) { fill: var(--n-950) !important; }

  @media (max-width: 1000px) {
    .logoset, .panels, .rules, .voice { grid-template-columns: 1fr; }
    .dont { grid-template-columns: 1fr 1fr; }
    .fspec { width: 100%; }
  }
  @media (max-width: 900px) { .bhero { padding: var(--space-3xl) 0 0; } }
  @media (max-width: 560px) {
    .dont { grid-template-columns: 1fr; }
    /* Formats: the mockups are fixed export-pixel designs shrunk by a fixed
       transform into fixed-width wrappers, so they don't reflow on phones and
       the 1920-wide lower-third overran the column. Shrink wrapper + scale in
       lockstep so every format fits a ~320px-wide screen (content ≈ 280px). */
    .fmt-stage { gap: var(--space-lg); }
    .w360 { width: 260px; height: 260px; } .b1x1 { transform: scale(.24074); }
    .w270 { width: 220px; height: 391px; } .b9x16 { transform: scale(.2037); }
    .w480 { width: 260px; height: 146px; } .b16x9 { transform: scale(.13542); }
  }
</style>
