# reference mockups (visual source, not shipped)
_reference/

# dependencies
node_modules/

# build output
dist/

# astro
.astro/

# cloudflare
.wrangler/

# env / secrets
.env
.env.production
.dev.vars

# os / editor
.DS_Store
Thumbs.db
*.log
npm-debug.log*

# claude.ai/design sync — account-specific tooling, not site source (public repo keeps this out, like _reference/)
ds-bundle/
.design-sync/

# perf: raw Lighthouse runs (regenerate via scripts/perf/run.sh)
scripts/perf/out/
